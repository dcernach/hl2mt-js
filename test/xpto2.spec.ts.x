import { expect } from "chai";
import * as md5 from "../src/core/md5";

xdescribe('Array', function () {

    xit('should return 2 when the value is present @index 1', function () {
        expect([1, 2, 3].indexOf(3)).to.be.eq(2);
    });
    
});